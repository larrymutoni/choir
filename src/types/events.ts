export type EventItem = {
  id: string;
  title: string;
  description: string;
  event_date: string;
  location: string;
  image_path: string | null;
  is_visible: boolean;
  created_at: string;
  updated_at: string;
};
